# dependencies
node_modules/

# build output
dist/
.astro/

# python
api/.venv/
api/__pycache__/
api/**/__pycache__/
api/**/*.pyc
api/.pytest_cache/
api/.coverage
api/htmlcov/
api/.mypy_cache/
api/.ruff_cache/

# environment
.env
.env.local
.env.*.local

# logs
*.log
npm-debug.log*

# editor / OS
.DS_Store
Icon?
.vscode/
.idea/

# ansible
deploy/*.retry
# deploy/vault.yml is ansible-vault encrypted and SAFE to commit.
# An unencrypted vault.yml is NOT — refuse it.
# Detect with: git diff --cached | grep -E '^\+(vault_|password)'

# playwright session snapshots
.playwright-mcp/
