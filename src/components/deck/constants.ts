// Voice-friendly placeholder reused across text inputs + the note field —
// nudges mobile users toward the keyboard's dictation mic.
export const VOICE_PLACEHOLDER = "Add a note. Tap the keyboard mic to talk.";

// File-upload limits (mirrors app.ts). Used once file-upload lands.
export const MAX_FILES_PER_CARD = 5;
export const MAX_FILE_BYTES = 25 * 1024 * 1024;
